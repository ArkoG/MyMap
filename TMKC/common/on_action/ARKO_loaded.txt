#global flag for the mod, can help mod compatibility

on_game_start = {
    on_actions = {
        on_mod_start
    }
}
on_mod_start = {
    effect = {
        set_global_variable = {
            name = ARKOMOD_is_loaded
            value = yes
        }
    }
}